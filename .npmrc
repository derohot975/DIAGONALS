# Production-only configuration for platform-specific binaries
# This file is used ONLY in CI/CD production builds to reduce bundle size
# Development environment should NOT use this file

# Target Linux x64 for production deployment (Render.com uses Ubuntu)
target_platform=linux
target_arch=x64

# Only install binaries for the target platform in production
# This excludes darwin-x64, darwin-arm64, win32-x64, etc.
# Estimated savings: ~57.6 MB for packages like esbuild, lightningcss
optionalDependencies=false
